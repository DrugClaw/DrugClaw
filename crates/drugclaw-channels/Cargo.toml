[package]
name = "drugclaw-channels"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
drugclaw-storage = { path = "../drugclaw-storage" }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
